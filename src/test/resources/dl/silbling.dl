%---------------
% facts
parent(alice, bob).
parent(alice, bart).
parent(alice, betty).

% rules
sibling(X, Y) :- parent(A, X), parent(A, Y), X <> Y.

% queries
sibling(A,B)?
sibling(bob,B)?
